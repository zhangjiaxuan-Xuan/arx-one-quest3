# ARX 功能等级检阅

## 检阅结论

| 功能 | 等级 | 断链/异常行为 | 结论 |
|---|---:|---|---|
| 双臂 SDK、厂商后台收发、限位 | L4 | 常驻且动作写入串行化；最终回停机位后才断开 | 通过 |
| Ctrl-C、TERM、HUP、未处理异常 | L3 | 撤销 VR；锁定安全停机；重复信号不能打断回停 | 通过 |
| 电脑采集/审核命令 | L2 | 只能向状态机请求；异常自动进入安全停机 | 通过 |
| Quest 位姿、Grip、Trigger、按钮 | L1 | 丢包停止生成目标；恢复后从实测状态重新锚定；不得管理 SDK | 通过 |
| Quest UDP socket/IP/序列号 | L1 | 自动恢复；失败不向电脑层抛出终止异常 | 通过 |
| 审核阶段 | L2 | 回停机位低阻尼；保持同一 SDK 在线 | 通过 |
| 旧式直接采集/回放/远程执行 | 不合规 | 会绕过统一常驻命令会话 | 已阻断 |

## 抢占顺序

```text
L4 机械臂常驻 SDK 会话
  > L3 异常与终端安全停机
    > L2 电脑工作流
      > L1 VR 输入
```

低等级只能提交请求，不能清除高等级状态。安全停机闩锁一旦置位，VR 和电脑
工作流在本进程内都不能重新开放运动。

## 故障隔离要求

- Quest 丢包、追踪丢失、应用重启、IP 改变或序列号归零：应用层立即停止
  生成机械臂命令，厂商 SDK 会话保持；恢复后以当前手柄和当前机械臂状态重新锚定。
- Quest 接收线程异常：VR 显示离线，电脑键盘和机械臂 SDK 会话继续运行。
- 单侧 VR 数据异常：只冻结对应侧，不终止另一侧。
- 电脑终端断开或 SSH 挂断：`SIGHUP` 进入与 Ctrl-C 相同的安全回停路径。
- 回停机位失败但反馈有效：保持厂商阻尼 SDK 连接并由操作者处理，禁止直接析构控制器。

## 尚未开放的功能

模型 action 的直接机械臂执行目前保持禁用。后续部署适配必须把模型 action
作为与 VR 相同的低等级输入源接入 `SafetyAuthority`，由同一常驻 SDK 会话、
限位器和停机状态机执行，不能恢复旧脚本自行连接机械臂的方式。

## 模型部署前的硬阻断项

- `convert_pi05.py` 与远程适配器现已统一为
  `arx_ac_one.bimanual_delta_eef.v1`（右/左末端 6D 下一步微分 + 二值夹爪）；
  π0.5 内部将 14 维补零到 32 维，机器人边界只允许反归一化后的 14 维 schema。
- 当前 LeRobot 转换明确不包含三路相机，`training_ready=false`。原始 NUT 视频和
  `time_alignment.npz` 已保真保存，但还需要按对齐索引生成三视觉训练数据。
- 模型执行器必须复用 `PersistentArms`、`command_gate`、`sdk_lock`、健康监控、命令限幅、夹爪保护、
  `SafetyAuthority` 和停机位状态机；在完成该迁移前，旧执行入口持续阻断。
