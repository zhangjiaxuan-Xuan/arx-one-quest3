# ARX AC One 双臂控制生命周期

## 核心不变量

1. 一次程序运行只建立一组双臂 SDK 控制器，等待、采集、Quest 断链和审核阶段均复用该会话。
2. 程序启动时机械臂必须位于本次开机记录的停机位。正式 SDK 会话首先调用厂商阻尼模式，连续只读校验双臂状态时间戳，通过后才允许运动；应用层不得另建命令心跳。
3. 采集起始位与停机位是两个不同姿态。开始采集前回到共享采集起始位；审核和最终退出前回到停机位。
4. Quest 丢包、应用重启、序列号归零、IP 变化或单手柄追踪丢失，不得析构 SDK、切断 CAN 或退出电机控制。
5. 最终退出只有一条合法路径：停止命令生产者 → 回停机位 → 验证误差 → 低阻尼 → 关闭 SDK。
6. 未确认停机位时禁止关闭 SDK。CAN反馈短暂停顿时冻结新目标并保持同一SDK会话；连续失联5秒后停止位置控制、请求低阻尼重力补偿并由操作者手动扶正。

## 控制权限等级

| 等级 | 权限 | 可以做什么 | 明确禁止 |
|---|---|---|---|
| L4 机械臂 SDK 会话 | 最高 | SDK 常驻、串行化动作写入、限位、模式切换、回停机位、最终断开 | 被电脑或 VR 覆盖 |
| L3 异常与信号安全 | 次高 | 将安全停机闩锁置位、撤销所有下级运动授权 | 清除 SDK 生命周期规则 |
| L2 电脑工作流 | 普通 | 请求开始/结束采集、审核、接受/拒绝 | 直接关闭 SDK、绕过停机位 |
| L1 VR | 最低 | 在授权窗口提交腕部目标、夹爪意图和工作流请求 | 切增益、管理 CAN/SDK、阻止停机 |

安全停机闩锁是单向的：一旦终端信号或上级异常置位，本进程内电脑和 VR 都不能重新开放运动，只能完成回停机位并退出。

## 启动阶段

1. 根据固定 CANable 序列号恢复 `can0=左臂`、`can1=右臂`。
   使用经过双轮手持生命周期验证的 stock SLCAN 参数
   `slcand -o -f -s8`；不得未经硬件耐久验证强制修改 USB CDC line coding
   或扩大 TX 队列。
2. SDK 前预检只检查接口存在、接口已启用和 RX 错误没有增长。ARX 电机由 SDK 主动查询，空闲接口 0 RX 帧是正常状态，不能据此判定电机断线。
   双侧同时 idle 或同时 active 均可接受；一侧 active、一侧 idle 时禁止加载 SDK，
   必须先运行 `sudo ./tools/recover_arx_can.sh` 仅重建 SLCAN。普通启动恢复不得使用
   `--usb-reset`，后者只用于 SLCAN 重建仍失败时。
3. 创建正式 `PersistentArms`，左右臂均启用重力补偿。X5 厂商库强制
   `shutdown_to_passive=true`，因此控制器对象必须常驻，只能在确认停机姿态后析构；
   初始化顺序固定为先左臂 `can0`、后右臂 `can1`。两轮独立生命周期基线测试已验证：
   此顺序下两侧反馈连续且可重复正常重连；右臂单独初始化或先于左臂初始化时，
   `can1` 可能在启动后停止 RX、只剩 TX。禁止改变此顺序。
4. 在停机位连续读取 15 组关节、速度、力矩和夹爪状态，检查有限值和厂商状态时间戳，只读验证停机位关节误差不超过 0.35 rad。这里不发送位置命令。
5. 预检通过后沿用同一 SDK 会话移动到共享采集起始位。
   启动回位和最终回停机位执行两级CAN容错：1秒内的瞬时调度抖动不触发恢复写入；检测到停顿后停止生成新目标并被动等待。5秒内恢复则从实测关节重新建立命令起点并继续；连续5秒失联才转人工处理。

## Quest 跟随阶段

- 可选前向校准：等待阶段松开双 Grip，按住左 Menu，同时将左手柄沿操作者认定的正前方移动 1 秒。程序用该水平位移计算 yaw 补偿，并把它旋转到既有固定映射的 Quest `+X` 前方。
- 前向校准只作用于三轴平移；上下轴、腕部旋转映射、1.25 缩放和固定 `quest_to_robot` 配置不变。未校准时仍使用原始固定映射。
- 左右 Grip 是纯软件离合器，分别授权对应机械臂；Trigger 控制对应夹爪。
- Grip 首次授权时夹爪意图必须直接取自 Trigger，禁止根据实测夹爪宽度猜测开合状态；Trigger 未按严格对应数据规范 `0=最大张开`，按下对应 `1=闭合`。
- 共享采集初始姿态只从实测状态注册六轴关节；左右夹爪字段始终强制为 `0.082 m` 完全张开。任何回采集初始位流程都必须等实测夹爪张开后才报告完成，停机姿态不受此规则影响。
- 采集中松开对应 Grip 后，左手柄 X 仅让左臂、右手柄 A 仅让右臂以安全限速回到共享采集初始位并完全张开对应夹爪；另一臂继续受控，回位轨迹继续写入本轮数据。
- 单臂回位期间该侧 VR 位姿输入被屏蔽；到位后保持标准姿态，下一次按 Grip 时从实测姿态重新锚定。左 X + 右 B 的全局安全退出组合键优先，禁止误触发左臂回位。
- Grip 松开后不再生成应用层位置命令，不切换增益、不析构 SDK；再次按下时从实测状态重新锚定。
- 手柄平移和旋转组成同一个 6D 腕部目标，由整末端 IK 一体解算到六个关节；不得重新拆成两个彼此独立的关节组。
- 每次 IK 解先经过 80 ms 一阶低通，再由 0.75 rad/s 速度上限、8 rad/s²
  加速度上限和制动距离规划生成关节命令。目标反向时必须先减速到零再反向，
  禁止单纯位置限幅产生 `+max_step → -max_step` 的单周期抽动。
- Quest 丢包期间立即停止 VR 命令生产，由厂商 SDK 的后台收发维持当前模式；应用层不得重发最后命令。
- 数据恢复后读取当前机械臂姿态和当前手柄姿态，重新锚定后继续；断链期间的手柄移动不追赶，防止恢复跳变。
- 左右臂异常隔离；单侧瞬时读写异常不会终止另一侧控制线程。
- 任一路 CAN 出现持续反馈停顿时，双臂停止接收新的 VR 目标；不得发送“恢复心跳”、析构 SDK 或自动追赶旧轨迹。原连接在5秒内恢复时，从双臂实测姿态重新锚定后继续。
- CAN连续失联达到5秒才闩锁安全退出。反馈不可验证时程序停止位置目标，不再盲写模式或回归轨迹；操作者物理支撑并关闭控制电源后按 Enter 或 Ctrl-C 释放 SDK。

## 等待与审核阶段

- 等待采集时保持 SDK 在线和低阻尼重力补偿。
- 进入审核前回到停机位并设置低阻尼，但不析构 SDK。
- Quest 和视频流可以在审核期间断开；机械臂 SDK 会话继续保持。
- 继续采集时沿用同一连接回到共享采集起始位。

## Ctrl-C 与最终退出

- 厂商SDK尚未成功返回任何机械臂控制对象时，第一次Ctrl-C中断初始化；若构造函数卡死，第二次Ctrl-C立即结束，因为此时不存在可执行回位的控制对象。
- 第一次 Ctrl-C/TERM/USR1 只请求安全停机。
- 开始回停机位后锁住信号，后续 Ctrl-C 不会中断轨迹或强杀 Python。
- 外层 shell 保存真实 Python PID，不保存 `tee` PID。
- 外层启动器等待 `SAFE_SHUTDOWN_COMPLETE` 握手；没有该标志时不主动强杀机器人进程。
- 反馈仍有效但回停误差不合格时拒绝关闭SDK并保持低阻尼；反馈失效时禁止盲目重复回位。

## 启动命令

所有一键正式采集会先自动运行 16 秒无机械臂合成输出闸门。该阶段不构造
ARX 控制器，验证当前 Quest 映射、PID、IK、夹爪逻辑和 50 Hz 调度没有突跳、
多写者或丢包后追赶；失败时在 CAN/SDK 连接前终止。

需要使用真实 Quest 做较长的软件输出测试时（机械臂可以断电）：

```bash
./tools/run_quest3_output_stability_test.sh live 180
```

如需固定 Quest 来源，可在末尾增加 `--quest-host <QUEST_IP>`。测试时请依次操作
双 Grip、三轴平移、三轴旋转、Trigger，并主动让头显短暂休眠后恢复。

官方旧基线通过后，先验证项目常驻封装（不启动 Quest、相机或位置命令）：

```bash
./tools/run_persistent_session_passive_test.sh 180
```

只有该 A/B 第二阶段同样稳定，才进入完整采集测试。

完整采集：

```bash
cd /path/to/arx-ac-one-pi05
./tools/start_quest3_collection_test.sh --task <task_text>
```

限时双臂测试：

```bash
./tools/start_quest3_bimanual_test.sh 60
```

CANable 重新插拔或 USB 错误后：

- 仅重建 slcan：`sudo tools/recover_arx_can.sh`
- 在没有任何机械臂控制进程时，按固定序列号复位两只 CANable 固件并重建
  slcan：`sudo tools/recover_arx_can.sh --usb-reset`
- USB 固件复位是机械臂断电前的最后一级软件恢复；如果复位后某一 CAN 总线仍无
  电机响应，不能继续反复构造厂商 SDK，必须检查机械臂供电/物理 CAN 或重新上电。

```bash
sudo ./tools/recover_arx_can.sh
```

## 禁止事项

- 不在正式启动前运行一次性 SDK 电机探针；探针析构可能让电机需要重新上电。
- 不使用 `kill -9`、直接关闭终端或杀外层 `tee` 代替安全退出。
- 不在审核阶段关闭并重建双臂 SDK。
- 不把采集起始位当作最终停机位。
- `capture_demo.py`、`replay_pi05.py` 和 `remote_delta_roundtrip.py --execute`
  的旧式直接硬件入口已禁用；在模型执行迁入同一常驻命令会话前不得恢复。
